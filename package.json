{
  "name": "nodejs-proxy",
  "version": "1.0.0",
  "description": "",
  "main": "apps.js",
  "scripts": {
    "start": "node apps.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "http-proxy": "latest",
    "ws": "^8.16.0"
  }
}
